import { SvelteComponentTyped } from "svelte";

declare module 'svelte-fullscreen' {
	interface FullscreenProps {
	}

	class Fullscreen extends SvelteComponentTyped<
		FullscreenProps,
		{ change: CustomEvent<never>, error: CustomEvent<never> },
		{ onToggle: any, onRequest: any, onExit: any }
	> {}

	export default Fullscreen;
}

