declare type packageInfo = string | RegExp;
declare type Strategy = 'default' | 'all-in-one' | 'single-vendor' | 'unbundle';
export declare type CustomSplitting = Record<string, packageInfo[]>;
export interface ChunkSplit {
    strategy?: Strategy;
    customSplitting?: CustomSplitting;
}
export {};
